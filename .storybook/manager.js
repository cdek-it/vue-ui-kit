import { create } from 'storybook/theming';
import './newLabel.css';

export const theme = create({
  base: 'light',
  brandTitle: 'CDEK UI Kit',
  brandUrl: '',
  brandImage: '/CDEK.svg',
  brandTarget: '_self',
});
