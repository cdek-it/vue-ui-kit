import { Meta, Story } from '@storybook/addon-docs';

import { Template, OverlayTemplate, WithoutHeaderTemplate, WithoutFooterTemplate, WithoutCaptionTemplate } from './Card.template';

<Meta title="Prime/Panel/Card" />

# Card

[PrimeVue Card](https://primevue.org/card), [Макет](https://www.figma.com/design/HOLKdvQJ8jCLeX17s9d0Yf/UI-Kit--DS--v2.0?node-id=1213-4134)

Card компонент используется для группировки контента с заголовком, подзаголовком и действиями.

## Полная карточка

Карточка со всеми элементами: header, title, subtitle, content и footer.

```html dark
<PrimeCard style="width: 240px">
  <template #header>
    <img src="image.jpg" alt="header" />
  </template>
  
  <template #title>
    Title
  </template>
  
  <template #subtitle>
    Caption
  </template>
  
  <template #content>
    Card content
  </template>
  
  <template #footer>
    <Button label="Action" />
  </template>
</PrimeCard>
```

<Story name="Default">{Template.bind({})}</Story>

## Overlay варианты

Карточка может отображаться с тенью (overlay: true) или без тени (overlay: false).

```html dark
<!-- С тенью -->
<PrimeCard :pt="{ root: { class: 'shadow-md' } }">
  <!-- content -->
</PrimeCard>

<!-- Без тени -->
<PrimeCard :pt="{ root: { class: 'shadow-none' } }">
  <!-- content -->
</PrimeCard>
```

<Story name="Overlay">{OverlayTemplate.bind({})}</Story>

## Без header

Карточка без изображения в шапке.

<Story name="Without Header">{WithoutHeaderTemplate.bind({})}</Story>

## Без footer

Карточка без футера с действиями.

<Story name="Without Footer">{WithoutFooterTemplate.bind({})}</Story>

## Без caption

Карточка без подзаголовка (subtitle).

<Story name="Without Caption">{WithoutCaptionTemplate.bind({})}</Story>
