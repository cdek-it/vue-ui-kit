import { Meta, Story, Canvas, Controls, Title, Description } from '@storybook/addon-docs/blocks';
import * as InputNumberStories from './InputNumber.stories';

<Meta of={InputNumberStories} />

<Title />
<Description />

## Импорт
```js
import InputNumber from 'primevue/inputnumber';
```

## Варианты использования

### Default
Ввод числовых значений с кнопками увеличения/уменьшения. Управляйте состоянием (размер, валидность, доступность) через таблицу аргументов ниже.
<Canvas of={InputNumberStories.Default} />

#### Список параметров
<Controls of={InputNumberStories.Default} />

### Float Label
Использование поля ввода внутри контейнера `FloatLabel`.
<Canvas of={InputNumberStories.FloatLabel} />

### Валюта
Форматирование значения в виде валюты через `mode="currency"`.
<Canvas of={InputNumberStories.Currency} />

### Min/Max и Step
Ограничения диапазона и шаг изменения значения.
<Canvas of={InputNumberStories.MinMax} />
