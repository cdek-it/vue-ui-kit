import { Meta, Story, Canvas, Controls, Title, Description } from '@storybook/addon-docs/blocks';
import * as MultiSelectStories from './MultiSelect.stories';

<Meta of={MultiSelectStories} />

<Title />
<Description />

## Импорт
```js
import MultiSelect from 'primevue/multiselect';
```

## Варианты использования

### Default
Множественный выбор с отображением значений через запятую. Управляйте состоянием и внешним видом через таблицу аргументов ниже.
<Canvas of={MultiSelectStories.Default} />

#### Список параметров
<Controls of={MultiSelectStories.Default} />

### Chips
Отображение выбранных значений в виде чипов (`display="chip"`).
<Canvas of={MultiSelectStories.Chips} />

### Float Label
Использование поля внутри контейнера `FloatLabel`.
<Canvas of={MultiSelectStories.FloatLabel} />
