import { Meta, Canvas, Title, Description, Controls } from '@storybook/addon-docs/blocks';
import * as InputMaskStories from './InputMask.stories';

<Meta of={InputMaskStories} title="Prime/Form/InputMask" />

<Title />

<Description />

```javascript
import InputMask from 'primevue/inputmask';
```

## Варианты использования

### Default

Ввод данных по строго заданной маске. Поддерживает динамическую смену размера и кнопку очистки через панель свойств.

<Canvas of={InputMaskStories.Default} />

<Controls of={InputMaskStories.Default} />

### Float Label

Пример использования с плавающим лейблом внутри поля.

<Canvas of={InputMaskStories.FloatLabel} />
