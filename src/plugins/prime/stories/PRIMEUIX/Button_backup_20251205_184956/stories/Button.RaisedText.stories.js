export default {
  title: 'PRIMEUIX/Button/RaisedText',
  parameters: {
    docs: {
      description: {
        component: `
Text buttons can be displayed as raised for elevation.
        `.trim()
      }
    }
  }
};

export const Default = () => ({
  template: `
    <div class="card flex flex-wrap justify-center gap-3 p-4">
<!-- No code example available for this section -->
    </div>
  `
});
