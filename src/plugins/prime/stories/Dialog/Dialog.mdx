import { Meta, Canvas, Title, Description, Controls } from '@storybook/addon-docs/blocks';
import * as DialogStories from './Dialog.stories';

<Meta of={DialogStories} />

<Title />
<Description />

```javascript
import Dialog from 'primevue/dialog';
```

## Варианты использования

### Basic
Базовый вариант модального окна с заголовком и кнопками действия.
<Canvas of={DialogStories.Basic} />

#### Параметры компонента
<Controls of={DialogStories.Basic} />

### Size
Различные предустановленные размеры окна: `small`, `base`, `large`, `xlarge`.
<Canvas of={DialogStories.Size} />

### No Modal
Отключение модального режима позволяет взаимодействовать с содержимым страницы, пока диалог открыт.

<Canvas of={DialogStories.NoModal} />

### Show Header
Заголовок можно скрыть с помощью пропса `showHeader: false`.
<Canvas of={DialogStories.NoHeader} />
